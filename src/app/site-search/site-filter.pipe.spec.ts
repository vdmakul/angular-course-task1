import { SiteFilterPipe } from './site-filter.pipe';

describe('SiteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SiteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
